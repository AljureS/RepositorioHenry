let math

//x elevado a la potencia de y
    math = Math.pow(2,3)
    //console.log(math);

//redondear deciamales, respectivamente: normal, hacia el menor y hacia el mayor 
    math = Math.round(0.49)
    math = Math.floor(7.89)
    math = Math.ceil(48.12)
    //console.log(math);

// para concer el maximo y el minimo
    math = Math.max(8,7,9,4) //9
    math = Math.min (8,9,8,1) //1
    //console.log(math);

//generar numero aleatorio 
    math = Math.random()
    console.log(math);
